# Sama Accounting — Python dependencies (production + local dev)
# Install: pip install -r requirements.txt

Django==5.0.2
djangorestframework==3.14.0
Pillow==10.2.0
reportlab==4.2.0
weasyprint==62.1

# PostgreSQL (PythonAnywhere paid plan — use on production):
psycopg2-binary==2.9.9

# MySQL (alternative on PythonAnywhere):
# mysqlclient==2.2.4
